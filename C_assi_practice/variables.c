#include<stdio.h>

int main(){
    int a; //var declare
    a = 2; //var initialization
    printf("%d", a);
    printf("\nwelcome");
    return 0;
}

// frist char or _ allowed at starting , white space not allowed
//no comma , var are case sensitive, var can be change