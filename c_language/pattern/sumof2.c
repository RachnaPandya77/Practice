#include <stdio.h>

int main()
{
    int x, y, sum;

    printf("Enter X: ");
    scanf("%d", &x);

    printf("Enter Y: ");
    scanf("%d", &y);

    sum = x + y;
    printf("Sum of num is : %d ", sum);

    return 0;
}